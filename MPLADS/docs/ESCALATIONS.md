# Escalations

`review_escalations` is an auditable foundation for monitoring follow-up. It records a case, reason, requester, priority, status, and creation time. It does not create automatic punitive escalation or external integrations.

Review summary metrics count active escalation records from the persisted review database and apply the same case scope as the caller.
