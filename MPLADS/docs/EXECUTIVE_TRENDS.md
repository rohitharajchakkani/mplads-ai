# Executive trends

Trends compare the two most recent completed persisted analytics runs. They expose current value, previous comparable value, absolute change, and percentage change only when the previous value is non-zero. No historical line or `0%` is invented when comparison is unavailable.

When fewer than two completed runs exist, the API returns: `Historical trend unavailable: only one completed analytical run is available.`
