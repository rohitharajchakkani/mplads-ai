# Frontend placeholder

The React/Vite public frontend begins after the twelve supplied datasets have passed inspection, manifest assignment, schema mapping, and canonical database design. It must consume versioned backend APIs and must not include static MPLADS records or fallback metrics.

