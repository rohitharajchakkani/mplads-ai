import { useEffect, useState } from "react";
import { ArrowUpRight, BarChart3, Landmark, Menu, Search, ShieldCheck, X } from "lucide-react";
import { Link, NavLink, Outlet, useLocation, useNavigate } from "react-router-dom";

const navigation = [
  ["Home", "/"], ["Dashboard", "/dashboard"], ["Works", "/works"], ["Search", "/search"], ["Monitoring", "/monitoring"], ["About", "/about"],
] as const;

function Logo() {
  return <Link to="/" className="group flex items-center gap-2.5 rounded-md focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue focus-visible:ring-offset-2" aria-label="MPLADS AI home"><span className="relative grid size-10 place-items-center overflow-hidden rounded-md bg-navy text-white shadow-sm transition-transform group-hover:scale-105"><Landmark className="size-5" aria-hidden="true" /><i className="absolute inset-x-0 bottom-0 h-1 bg-[#d97706]" /></span><span><span className="block text-base font-bold tracking-tight text-ink">MPLADS AI</span><span className="hidden text-[10px] font-semibold uppercase tracking-[0.13em] text-slate-500 sm:block">Data intelligence portal</span></span></Link>;
}

export function AppShell() {
  const navigate = useNavigate();
  const location = useLocation();
  const [mobileOpen, setMobileOpen] = useState(false);
  const [term, setTerm] = useState("");
  useEffect(() => { setMobileOpen(false); }, [location.pathname]);
  const submit = (event: React.FormEvent<HTMLFormElement>) => {
    event.preventDefault();
    const value = term.trim();
    if (value) navigate(`/search?q=${encodeURIComponent(value)}`);
  };
  return <div className="app-canvas min-h-screen text-slate-800"><a className="skip-link" href="#main-content">Skip to content</a><header className="sticky top-0 z-30 border-b border-line/90 bg-white/95 shadow-sm backdrop-blur"><div className="gov-rule h-1" aria-hidden="true" /><div className="page-container flex h-[4.5rem] items-center justify-between gap-5"><Logo /><nav className="hidden items-center gap-0.5 lg:flex" aria-label="Primary navigation">{navigation.map(([label, path]) => <NavLink key={path} to={path} end={path === "/"} className={({ isActive }) => `nav-link ${isActive ? "nav-link-active" : ""}`}>{label}</NavLink>)}</nav><form onSubmit={submit} className="hidden min-w-48 max-w-56 flex-1 lg:block" role="search"><label className="sr-only" htmlFor="desktop-search">Search MPLADS data</label><div className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-400" aria-hidden="true" /><input id="desktop-search" value={term} onChange={(event) => setTerm(event.target.value)} placeholder="Search MPs, works, places" className="h-9 w-full rounded-md border border-line bg-slate-50 pl-9 pr-3 text-sm outline-none transition focus:border-blue focus:bg-white focus:ring-2 focus:ring-blue/20" /></div></form><button type="button" className="grid size-10 place-items-center rounded-md border border-line text-ink lg:hidden" aria-label={mobileOpen ? "Close navigation" : "Open navigation"} aria-expanded={mobileOpen} onClick={() => setMobileOpen((open) => !open)}>{mobileOpen ? <X className="size-5" /> : <Menu className="size-5" />}</button></div>{mobileOpen && <div className="border-t border-line bg-white px-4 pb-4 pt-2 lg:hidden"><nav className="page-container grid gap-1" aria-label="Mobile navigation">{navigation.map(([label, path]) => <NavLink key={path} to={path} end={path === "/"} className={({ isActive }) => `nav-link ${isActive ? "nav-link-active" : ""}`}>{label}</NavLink>)}</nav><form onSubmit={submit} className="page-container mt-3" role="search"><label className="sr-only" htmlFor="mobile-search">Search MPLADS data</label><input id="mobile-search" value={term} onChange={(event) => setTerm(event.target.value)} placeholder="Search MPs, works, places" className="field-control" /></form></div>}</header><main id="main-content" className="page-container py-8 sm:py-10"><Outlet /></main><footer className="mt-10 border-t border-line bg-white"><div className="gov-rule h-1" aria-hidden="true" /><div className="page-container grid gap-8 py-9 text-sm text-slate-600 md:grid-cols-[1.4fr_1fr_1fr]"><div><div className="flex items-center gap-2 font-semibold text-ink"><BarChart3 className="size-4 text-blue" aria-hidden="true" />MPLADS AI</div><p className="mt-3 max-w-xl leading-6">An evidence-led public-data context and authorization-gated intelligence workspace for examining MPLADS implementation signals.</p><p className="mt-3 text-xs leading-5 text-slate-500">This independent data interface is not an official government portal.</p></div><div><p className="text-xs font-bold uppercase tracking-[0.14em] text-slate-500">Public context</p><div className="mt-3 grid gap-2"><Link className="footer-link" to="/dashboard">Dashboard</Link><Link className="footer-link" to="/works">Works</Link><Link className="footer-link" to="/search">Search data</Link><Link className="footer-link" to="/about">Methodology &amp; limitations</Link></div></div><div><p className="text-xs font-bold uppercase tracking-[0.14em] text-slate-500">Intelligence access</p><div className="mt-3 grid gap-2"><Link className="footer-link" to="/monitoring">Authorized monitoring <ArrowUpRight className="mb-0.5 inline size-3" aria-hidden="true" /></Link><span className="inline-flex items-center gap-1.5 text-xs text-slate-500"><ShieldCheck className="size-3.5 text-[#18794e]" aria-hidden="true" />Scope is enforced by the backend.</span></div></div></div></footer></div>;
}
